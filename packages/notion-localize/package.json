{
  "name": "notion-localize",
  "version": "0.1.2",
  "description": "use Notion as localization and translation management tool",
  "source": "src/index.ts",
  "main": "dist/main.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "scripts": {
    "watch": "parcel watch",
    "build": "parcel build --no-source-maps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^3.1.0"
  },
  "devDependencies": {
    "@parcel/packager-ts": "^2.0.1",
    "@parcel/transformer-typescript-types": "^2.0.1",
    "parcel": "^2.0.1",
    "typescript": "^4.5.2"
  },
  "targets": {
    "default": {
      "context": "node",
      "includeNodeModules": true,
      "sourceMap": false,
      "optimize": false,
      "isLibrary": true
    }
  },
  "engines": {
    "node": ">=12"
  }
}
